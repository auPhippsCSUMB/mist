<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'MIST' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(180deg, #d3eefb 0%, #eef8fd 100%);
            min-height: 100vh;
        }

        .page-shell {
            max-width: 1100px;
        }

        .soft-card {
            border: 0;
            border-radius: 0.85rem;
            box-shadow: 0 0.35rem 1.2rem rgba(0, 0, 0, 0.08);
        }

        .section-title {
            font-weight: 700;
            letter-spacing: 0.2px;
        }
    </style>
</head>
<body>
    <div class="container page-shell py-4 py-md-5">
        <%- include('nav') %>
